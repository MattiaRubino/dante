# Observed Reality & Evidence Cluster v0 Checkpoint

**Status:** PASS — current validated cluster baseline  
**Validated:** 2026-08-12  
**Validation standard:** Domain Validation Methodology v3  
**Workstream:** Core Domain Model v0  
**Branch:** `feature/domain-model`

## Scope

This checkpoint validates the Observed Reality & Evidence cluster as one integrated semantic system rather than as six isolated concepts.

Validated current baselines:

- Actual v0;
- Outcome v0;
- Observation v0;
- Confirmation v0;
- Evidence v0;
- Provenance v0.

The cluster is evaluated after all six concepts individually passed the v3 Core Semantic, Multi-Actor Compatibility and Cross-Concept gates.

## Result

**PASS.**

The integrated validation found:

```text
6 accepted concepts retained
0 structural reopenings
0 concept removals
0 justified concept merges
0 mandatory new primitives
2 cross-cluster hardenings incorporated
multiple explicit deferred dependencies
```

The two integrated hardenings are:

1. reported/asserted reality must remain distinct from established Actual;
2. Milestone attainment is evidence/evaluation-backed checkpoint state and must not become a duplicate source of actual reality.

Neither finding changes concept identity.

---

# 1. Integrated semantic map

The cluster answers different questions:

```text
Actual
How did this specific expectation resolve in reality?

Outcome
What result/disposition followed from that realization?

Observation
What was measured, perceived, reported, asserted or explicitly derived?

Confirmation
Who/what explicitly affirms this specific target version for this purpose?

Evidence
What information materially bears on this evaluation, in what direction/context?

Provenance
How did this record/material version come to exist or change?
```

A useful composition is:

```text
intended/expected subject
        ↓
      Actual
   ┌────┴────┐
   ↓         ↓
Outcome   Observation(s)
   │         │
   └────┬────┘
        ↓
optional Confirmation

eligible information
        ↓
Evidence role in a specific evaluation

Provenance cuts across records/material versions
and explains material origin/evolution.
```

This is not a mandatory pipeline and not a persistence schema.

---

# 2. Cluster Integration Gate

## CL-01 — Representative reconstruction

Representative workflows reconstructed naturally:

- planned run executed partially with multiple measurements;
- meeting occurs at a different time than scheduled;
- expected medication occurrence passes with no known realization;
- known non-performance confirmed later;
- spontaneous debugging Session with no prior Activity/Actual;
- exam result imported, corrected and used to evaluate a Goal/Milestone;
- external approval reaches a Milestone without synthetic Activity;
- provider disagreement preserved until reconciliation;
- AI/OCR extraction corrected by user;
- caregiver records reality about another Subject.

**Result:** PASS.

## CL-02 — Deep integrated chronology

Tested chronology:

```text
expectation
→ schedule
→ execution / non-execution
→ initial assertion/import
→ current Actual where establishable
→ Outcome/Observation
→ Confirmation where needed
→ Evidence use
→ correction/revision
→ later re-evaluation
→ historical query months later
```

The model can answer:

- what was expected then?;
- what was known then?;
- what was merely asserted?;
- what was established as Actual?;
- what Evidence was used?;
- what source/provenance existed?;
- what was corrected later?;
- what is current now?

**Result:** PASS WITH HARDENING — asserted reality vs established Actual must remain explicit.

## CL-03 — Cross-concept redundancy

Adversarial REMOVE/MERGE/SPLIT was rerun across the whole cluster.

```text
Actual + Session            -> merge rejected
Actual + Outcome            -> merge rejected
Actual + Observation        -> merge rejected
Outcome + Observation       -> merge rejected
Outcome + Milestone         -> merge rejected
Observation + Evidence      -> merge rejected
Observation + Confirmation  -> merge rejected
Confirmation + Provenance   -> merge rejected
Evidence + Provenance       -> merge rejected
```

No universal `Reality`, `Fact`, `Result`, `EvidenceRecord` or `EpistemicState` mega-object is justified.

**Result:** PASS.

## CL-04 — Top-down traceability

Example:

```text
Goal
→ Plan / Routine / Activity
→ Occurrence / Schedule
→ Session(s)
→ Actual
→ Outcome / Observation
→ Evidence
→ Goal/Milestone evaluation
```

No layer must be fabricated when unnecessary.

**Result:** PASS.

## CL-05 — Bottom-up reconstruction

Spontaneous/imported reality can exist first:

```text
Observation
Session
external record
```

Later semantic relevance may be discovered and used as Evidence without inventing a historical Goal/Plan/Activity/Routine.

**Result:** PASS.

## CL-06 — Lateral propagation

One source record may influence several contexts without duplication.

Example:

```text
Observation: walked 10.4 km
  ├─ Evidence for fitness criterion
  ├─ Evidence for weekly distance review
  └─ unrelated to other contexts
```

Direction/strength/context remain evaluation-specific.

**Result:** PASS.

## CL-07 — History / correction integrity

A correction changes current accepted representation without falsifying prior assertions, Confirmation targets, Evidence basis or Provenance.

Example:

```text
Observation v1 = 66.4 kg
used by Evaluation E1
confirmed by C1

later correction
Observation v2 = 64.6 kg

current evaluation may use v2
historical E1 still explains that it used v1
C1 does not silently transfer to v2
```

**Result:** PASS WITH HARDENING.

## CL-08 — Scale / product complexity

The cluster does not require every real-world fact to create:

```text
Actual + Outcome + Observation + Confirmation + Evidence + Provenance
```

Simple cases remain simple:

```text
weight measurement -> Observation
spontaneous work -> Session
ordinary meeting occurrence -> Event + Actual
```

Evidence/Provenance may be derived, materialized or represented at bounded granularity according to history/materiality needs.

**Result:** PASS.

---

# 3. Critical integrated hardening — asserted reality vs established Actual

A report, provider feed, device reading, user statement or AI extraction may describe reality without automatically becoming the current established Actual.

Scenario:

```text
Actor A: medication taken
Actor B: medication not taken
Device: dispenser unopened
```

Required representation may preserve three source assertions/Observations with Provenance and optional Confirmations while Actual remains unresolved.

Canonical rule:

> **Reported/asserted reality != established Actual. Conflicting assertions must remain representable without manufacturing several competing canonical Actuals or forcing premature resolution.**

Actual is established only under the applicable reconciliation/authority/policy context.

Unknown may remain unknown.

---

# 4. Critical integrated hardening — Milestone attainment

Milestone remains distinct from Outcome and Evidence.

Example:

```text
Event: design review
Actual: review occurred
Outcome: approved
Evidence: approval result supports checkpoint
Milestone: Design approved -> attained
```

The Milestone does not need a parallel independent copy of the underlying reality.

Canonical rule:

> **Milestone attainment is evidence/evaluation-backed checkpoint state. It must not become a second independent source of Actual, Outcome or Observation truth.**

A reached/effective time may later be derived or materialized for query/history, but its basis must remain explainable.

---

# 5. Cluster Multi-Actor Stress Gate

## A — Shared meeting, different attendance intervals

One shared Event/Actual remains compatible with actor-specific participation intervals.

**PASS — Participation deferred.**

## B — Shared Activity, multiple performers, partial execution

Shared realization and Outcome do not require identical actor contribution.

**PASS — Responsibility/Participation deferred.**

## C — Shift hand-off

Planned performer may differ from actual performer. Acceptance, Authority, Actual and Outcome remain distinct.

**PASS — Authority/Responsibility deferred.**

## D — Caregiving

Subject, source actor, recorder, observer, performer, confirmer and authority can differ.

**PASS — Subject/Actor model deferred.**

## E — Conflicting reality

Actor/provider claims can coexist without silent overwrite or averaging.

**PASS WITH HARDENING — asserted reality != established Actual.**

## F — External/non-LifeOS participant

External people/providers can be represented without synthetic accounts.

**PASS.**

## G — Privacy

Shared Actual/derived consequence may coexist with private Observation/Evidence/Provenance.

**PASS.**

## H — Correction

Later correction preserves prior assertion, target version, Evidence basis and Provenance.

**PASS.**

## I — Collaborative Session

One logical Session may have different actor participation intervals.

**PASS — Participation model deferred.**

## J — AI on private multi-party context

AI may reason from authorized private context but cannot create disclosure permission, human Confirmation, canonical Actual or Authority.

**PASS WITH HARDENING.**

---

# 6. Multi-actor cluster invariants

```text
shared Actual != identical actor participation
shared Outcome != identical actor consequence
assertion by actor A != group truth
source/recorder/confirmer != Authority
private Evidence use != disclosure permission
target visibility != Evidence visibility != Provenance visibility
current access != historical attribution
AI knowledge != disclosure permission
AI inference != Confirmation
AI inference != established Actual
```

No duplicate collaboration-specific Reality ontology is justified.

---

# 7. Deferred dependencies

These are explicit downstream dependencies, not cluster failures.

## Data / Subjects

- Quantity;
- Register;
- Subject;
- Person / Actor boundary;
- Resource.

Re-tests:

- Observation vs Quantity;
- Observation vs Register;
- subject vs observer/recorder/source;
- high-frequency sampled data;
- Actor vs Subject vs Resource vs Account/Principal.

## Relationships / Reasoning

- Participation;
- Responsibility / Assignment / Hand-off;
- Authority;
- Visibility;
- GoalCriterion/evaluation relationship;
- Version;
- Decision;
- Audit boundary;
- typed Relationship.

Re-tests:

- Actual establishment under conflicting assertions;
- Confirmation vs Authority;
- Evidence vs typed Relationship;
- Provenance vs Version/Decision/Audit;
- Milestone attainment basis/history;
- actor-specific collaborative execution.

---

# 8. Rejected cluster alternatives

Rejected:

- one universal Reality object;
- one global status/result enum;
- Session as universal Actual;
- Observation as universal fact row;
- Outcome as universal completion state;
- Confirmation as generic truth flag;
- Evidence as duplicated data;
- Provenance as `source` string only;
- universal exhaustive Provenance graph;
- per-user duplicate shared reality records;
- absence of data interpreted as negative truth;
- AI inference interpreted as confirmed reality.

---

# 9. Cluster verdict

## Observed Reality & Evidence v0 — PASS

All six concepts remain accepted current baselines after integrated semantic, history, scale, privacy and multi-actor stress.

```text
Actual        retained
Outcome       retained
Observation   retained
Confirmation  retained
Evidence      retained
Provenance    retained

Structural reopenings    0
Concept removals          0
Concept merges            0
Mandatory new primitives  0
```

The cluster remains reopenable when later Data/Subjects, Relationships/Reasoning, persistence/API or implementation evidence exposes a real contradiction.

---

# 2026-08-13 — Downstream closure: Version / Material-State v0

This historical cluster checkpoint explicitly deferred Version. Version / Material-State v0 now closes the semantic state-binding dependency while preserving the original cluster validation record.

The original correction scenario:

```text
Observation v1 = 66.4 kg
Evaluation E1 used v1
Confirmation C1 targeted v1

later
Observation v2 = 64.6 kg
```

now has a shared canonical discipline:

- Observation identity may remain stable while a materially different state is created by correction;
- C1 remains bound to v1 and does not silently transfer to materially changed v2;
- historical E1 remains explainable from the source/rule material states used then;
- current evaluation may use v2 without rewriting E1;
- Provenance explains how v2 arose from/corrected v1, while Version identifies the state to which semantic actions/evaluations applied;
- Actual/Outcome corrections follow the same state/history discipline without Version deciding which assertion becomes current truth;
- concurrent provider/user states may coexist until Authority/Decision/reconciliation resolves or preserves the conflict;
- materiality is purpose-specific rather than every storage/provider/ETag revision.

Therefore:

```text
Version != Provenance
Version != Confirmation
Version != Evidence
Version != Decision / Authority / reconciliation
technical revision != semantic material state
current state != historical evaluation basis
```

Normative downstream references:

- `../concepts/version.md`;
- `version-material-equivalence-v0-validation.md`.

The Observed Reality & Evidence cluster remains **PASS**, with `REOPEN = 0`. Detailed source precedence/reconciliation policy and exact persistence remain independently SAFE DEFERRED.

---

# 2026-08-13 — Downstream closure: Reconciliation / Source Precedence v0

Reconciliation v0 resolves the cluster's historical detailed source-precedence/reconciliation dependency while preserving the original cluster verdict and its two integrated hardenings.

Current integrated separation:

```text
asserted/reported reality
!= established Actual

competing material states/assertions
!= Reconciliation process
!= current/effective state

Evidence
!= winner selection

Provenance
!= source precedence

Authority
!= objective truth

Version
!= reconciliation
```

Reconciliation now owns the cross-cutting process discipline for preserving and handling material competition. It may leave conflict unresolved, apply bounded specialist/source-of-record policy, use Evidence/Confirmation/Provenance, culminate in Decision, combine semantically independent changes, or deterministically establish/correct a current state under already-authorized policy. The affected domain concept still owns the current/effective state.

The original cluster conflict cases therefore no longer depend on an unnamed future mechanism. Competing Actual assertions, Observations, Outcome claims, Confirmations, Evidence and provider states can remain separately attributable while a bounded reconciliation basis determines whether and how current state changes.

Universal last-write-wins, newest-source-wins, provider-always-wins, user-always-wins, silent averaging and source identity as truth remain rejected. A specialist source-of-record has precedence only in its actual bounded authoritative context.

Historical assertions and material states remain reconstructible after resolution/correction. Conflict/source/Evidence/Provenance/rationale Visibility is independently governed, and AI confidence cannot create Authority or universal precedence.

Remaining independently SAFE DEFERRED debt includes GoalCriterion/evaluation, per-domain precedence rules, native identity merge/split, Trigger/policy mechanics, Principal/enforcement, specialist mappings, retention/audit and physical/API representation.

The Observed Reality & Evidence cluster remains **PASS**, `REOPEN = 0`; no concept is removed or merged.

Normative downstream references:

- `../concepts/reconciliation.md`;
- `reconciliation-source-precedence-v0-validation.md`.

---

# 2026-08-15 — Downstream closure: Criterion / Evaluation v0

Criterion / Evaluation v0 closes the cluster's historical GoalCriterion/evaluation semantic dependency while preserving the original Observed Reality & Evidence validation and its two integrated hardenings.

Current integrated separation:

```text
Actual / Outcome / Observation
= underlying reality/result/assertion semantics

Evidence
= contextual evaluative use of relevant information

Criterion
= contextual evaluative specification/capability

Evaluation
= contextual application of applicable Criterion state to relevant Evidence
  under material target/rule/context/time

Reconciliation
= contextual handling of materially competing states/assertions

Version / Material-State
= reconstructible state binding where consequence requires it
```

This closes the prior unnamed evaluation boundary without collapsing any accepted concept:

- `Criterion != Evidence`; the rule/question and the information used to assess it remain distinct;
- `Evaluation != Evidence`, `Actual`, `Outcome`, `Observation`, `Confirmation`, `Provenance` or `Reconciliation`;
- absence of Evidence does not establish non-occurrence, failure or a false Criterion result; Evaluation may remain unknown/insufficient;
- conflicting Evidence may require Reconciliation or remain unresolved/indeterminate rather than being silently averaged or ranked by recency;
- Milestone attainment remains evaluation/evidence-backed checkpoint state, not duplicate underlying reality;
- consequential historical Evaluation remains reconstructible against the material target state, Criterion state, Evidence/source basis and evaluation context that actually applied;
- later Evidence correction, Reconciliation or Criterion change may produce a later Evaluation without rewriting the original source assertion, historical Evidence use or previous evaluation basis;
- private Evidence may yield an authorized bounded shareable result without exposing source data or reconciliation rationale;
- AI may discover candidate Evidence or calculate under an authorized Criterion but cannot fabricate source facts, Authority, Criterion adoption or certainty.

The cluster's original **PASS** remains unchanged. `REOPEN = 0`; no Actual, Outcome, Observation, Confirmation, Evidence or Provenance concept is removed or merged.

Weighting/admissibility policy, specialist evaluation models, Trigger/policy machinery, retention/materialization and physical/API representation remain separately owned downstream concerns.

Normative downstream references:

- `../concepts/criterion-evaluation.md`;
- `criterion-evaluation-v0-validation.md`.