<!-- LIFEOS-CANONICAL-CONTINUATION document="relationship-v0-validation.md" follows="relationship-v0-validation-part-2.md" -->
> **Canonical continuation of the single logical Relationship v0 validation checkpoint.** Earlier validation remains preserved; this physical continuation records Membership v0 downstream resolution only.

# 2026-08-16 — Membership relation-family resolution

Relationship v0 rejects a universal Relationship root and requires each reusable relation family to prove its own semantics. Membership v0 has now completed that review.

Current downstream classification:

```text
Membership
RESOLVED / ACCEPTED
SPECIFIC CONTEXTUAL RELATION FAMILY / CAPABILITY
```

The generic Relationship rule remains unchanged:

```text
specific truthful relation
> generic related_to wrapper
```

Membership answers a distinct bounded question:

> Does this native referent belong to this Collective in the relevant scope/material state?

Required non-collapse:

```text
Membership != Participation
Membership != Responsibility
Membership != Coordination Stewardship
Membership != Authority
Membership != Visibility
Membership != Agreement / Consent
Membership != Account/security membership by default
```

Direct-vs-qualified rule:

- use direct specific Membership where the relation is semantically complete and carries no material independent relation state;
- qualify Membership where scope, lifecycle, role/facet, history, provenance, governance or visibility materially matters;
- a relationship row, M:N cardinality, graph traversal or query frequency does not create independent domain identity.

Rejected consequences:

```text
universal Relationship root
universal Membership entity/root
universal Member wrapper entity
GroupMember supertype
M:N join row = semantic primitive
```

Historical Membership state may remain reconstructible where consequential without changing this anti-wrapper rule.

Relationship v0 remains **PASS WITH HARDENING; REOPEN = 0; UNCLASSIFIED = 0**.

Normative downstream references:

- `../concepts/membership.md`;
- `../concepts/collective.md`;
- `collective-membership-quorum-v0-validation.md`.
