# DANTE — World Focus Current Checkpoint

**Status:** CURRENT LIVE WORLD FOCUS CHECKPOINT  
**Date:** 2026-09-01  
**Branch:** `feature/home-react`  
**Worktree:** `/home/mattia/projects/dante-frontend`

This is the first document a new chat/agent must read when continuing World Focus.

## 1. Current product state

```text
WF0 structural route/shell            FROZEN / USER AUTHORIZED
WF-G3 geometry                        LOCKED / USER AUTHORIZED
WF-V4 visual treatment                CANDIDATE / visual approval not frozen
B0 production foundation              ENGINEERING CLOSED
WR0 World product reverse engineering CLOSED
WR1 DANTE <-> user reverse engineering COMPLETE / 7 gaps found
WR2 gap closure                       CLOSED / 7 of 7 gaps closed
B1 Orientation                        CLOSED FOR SEQUENCING
B2 Continuity / Resume                IMPLEMENTED / AUTOMATED PASS
B2 integrated visual acceptance       DEFERRED
workspace/module scenario research    ALREADY DONE / RETAINED AS EVIDENCE
NEXT GATE                             MATERIALIZE WORLD WORKSPACE PLATFORM
```

No later World content mini-vertical is active.

## 2. Critical continuity rule — do not redo the workspace/module research

World Focus was already stress-tested against:

```text
unknown future World
unknown future specialist module
sparse World
dense World
large history
adaptive content changing around stable content
AI unavailable
provider stale/offline
late async results
sensitive/multi-actor content
layout/schema evolution
```

The recovered evidence is:

`world-focus-workspace-scenario-oracle-evidence.md`

It preserves the durable conclusions from the former WF0 scenario oracle without restoring its obsolete phase/roadmap status.

Therefore a new chat must **not** restart questions such as:

```text
“What if we do not know all future modules?”
“Do we need one page per World?”
“Can DANTE open deeper contextual surfaces?”
“Should AI be able to generate arbitrary UI?”
“Do stable/adaptive/ephemeral need separation?”
```

Those were already studied.

## 3. What was already decided about the workspace

Permanent direction:

```text
one World workspace platform
not one page per World
not one renderer family per World
not a generic Thing/WorldItem ontology
```

The workspace must support:

```text
question-driven dynamic composition
stable user-owned content
bounded adaptive content
ephemeral query/Insight content
finite registered renderers/surfaces
unknown future specialist surfaces through controlled extension
specialist renderers only when generic primitives materially lose meaning
typed source drill-down on demand
bounded/aggregated large-data projections
local failure isolation
responsive/container-owned presentation
```

AI/DANTE may drive contextual Insight/Explore/deeper-surface intents, but:

```text
no arbitrary model-generated JSX/HTML/JavaScript
no silent AI mutation of stable composition
AI Insight != persistent module automatically
```

Promotion into stable composition requires an accepted capability/configuration path.

## 4. Why B2 integrated acceptance is deferred

B2 established useful Continuity engineering and remains valid.

The user review exposed that judging more fixed-looking content inside the current empty rectangle is premature because the rectangle itself still lacks the real workspace orchestration and DANTE/surface footprint already anticipated by the platform studies.

Therefore:

```text
B2 engineering evidence remains valid
B2 is not discarded
B2 is not yet integrated/frozen visually
next content vertical does not start
```

First materialize the workspace platform around the already accepted semantics.

## 5. Critical correction: Home AI is not World DANTE

Do not reuse or copy the Home AI surface by analogy.

```text
HOME
cross-life compression/orientation/operation

WORLD FOCUS
scoped expansion, understanding and continuation of one continuity context
```

The underlying DANTE Intelligence capability may be shared, but product presentation, workspace behavior and contextual interaction are separate.

## 6. World product definition

Compass:

> **Understand this part of my life and continue from here.**

A World is a user-recognizable continuity context for a significant part of reality.

Core thesis:

> **A World is a shared coordinate system between the user and DANTE for one meaningful continuity context — not a shared source of truth.**

World is not:

```text
Domain owner
folder
life-area taxonomy
dashboard
universal entity container
DB partition
security boundary
AI memory bucket
chat room
mandatory Goal/time-range/KPI surface
```

## 7. World Output Grammar

World Focus is question-driven rather than widget-driven.

Possible output families:

```text
Orientation
Situation
Continuity / Resume
Attention / Resolution
Next
Change
Trajectory / Comparison  [only when meaningful]
Evidence / History
Explore
Act / Decide
Intelligence
```

A World does not need all of them. Sparse is valid. Missing answers are never filled with fake cards/KPIs/AI prose.

## 8. Dynamic composition rule

The interior is not a fixed page template.

```text
available authorized reality
-> which World questions have useful answers now?
-> ranking/composition
-> restrained current surface
```

Continuity, Next, Change, metrics, charts, people, artifacts and future specialist surfaces are possible answers/renderers, not mandatory permanent slots.

Stable user-owned content remains predictable. Adaptive/ephemeral content may surface bounded current value but may not silently rewrite stable composition.

## 9. DANTE semantic model already closed by WR2

Do not reopen casually:

```text
1. World identity / purpose
2. Stable World relevance definition
3. Current World interaction cursor/session when actually needed
4. Authorized purpose-scoped DANTE context built by Context Builder
```

World context biases relevance; it is not authorization.

DANTE may expand cross-World only when the user's purpose materially requires authorized broader context.

Visible projection and contextual DANTE result must preserve coherent basis/freshness semantics.

A run remains bound to its initiating World/cursor generation even if the user navigates elsewhere.

AI unavailable must not make the World unusable.

## 10. DANTE presence semantics already known

WR2 established presentation depths:

```text
P0 QUIET
P1 INVOKE
P2 CONTEXTUAL ENTRY
P3 INSIGHT
P4 PROPOSAL
P5 ACTION / RECEIPT
```

Earlier scenario work already demonstrated contextual requests that promote into Insight, Explore, source drill-down, comparisons and specialist surfaces where justified.

What remains open is **concrete workspace presentation/orchestration**, not whether those capabilities belong to the model.

## 11. Immediate next gate — materialize the World Workspace Platform

This is the active scope.

Do not redesign the outer route/sphere/VFX/opening now.

Work inside the frozen rectangular `worldFocus.workspace` and establish the production-grade frontend orchestration needed for the already-studied behavior.

The implementation must emerge from current contracts/evidence and cover, as proven necessary:

```text
composition host
finite surface/renderer registry
interaction cursor ownership
selected source/projection context
surface open / close / replace / promote semantics
Insight / Explore / contextual deeper-surface presentation
DANTE contextual presence and conversation footprint
stable / adaptive / ephemeral coexistence
focus / back / Escape ownership
responsive/mobile mapping
surface-local failure isolation
race/generation safety
performance/resource behavior
AI unavailable/degraded presentation
```

Important: do **not** create a giant speculative plugin framework or pre-enumerate every future surface. Build the smallest production-grade platform that satisfies the already-proven extension requirements.

### Still legitimately open

These concrete product/interaction decisions were never frozen and must be resolved while materializing the workspace:

```text
exact DANTE quiet footprint
composer placement
conversation expansion geometry
inline vs sidecar vs overlay/full-workspace mapping by semantic depth
surface coexistence/exclusivity
focus/back/Escape precedence
responsive/mobile behavior
exact local state model required by those interactions
```

These are implementation decisions inside a closed product/platform direction, not a reason to repeat the entire World/module reverse engineering.

## 12. Backend stop line

Current phase may build truthful frontend structure/interaction and deterministic adapters, but not:

- real API endpoints;
- DB/Alembic changes;
- provider integration;
- real model routing/streaming;
- durable Run/Task backend;
- real tool/effect execution;
- fake success pretending those exist.

The final backend vertical must plug into explicit application/interaction boundaries rather than require a workspace rewrite.

## 13. Read order

1. `world-focus-current-checkpoint.md`
2. `world-focus-handoff.md`
3. `world-focus-product-contract.md`
4. `world-focus-platform-contract.md`
5. `world-focus-structural-contract.md`
6. `world-focus-geometry-contract.md`
7. `world-focus-workspace-scenario-oracle-evidence.md` for the already-completed workspace/module uncertainty research
8. `world-focus-delivery-methodology.md`
9. `world-focus-frontend-roadmap.md`
10. `world-focus-evidence-index.md` for deeper archaeology.

## 14. User-gate rule

No mini-vertical/foundation gate closes or allows the next one merely because CI is green.

Required sequence:

```text
implementation
-> automated PASS
-> real browser/manual review
-> user functional test
-> user visual/interaction test
-> fixes + rerun
-> explicit user OK
-> freeze
-> next vertical
```

User acceptance is cumulative across the complete World surface built so far.
