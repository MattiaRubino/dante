# DANTE — World Focus Handoff

**Status:** CURRENT DURABLE HANDOFF — WS6 CLOSED / WS7 NEXT  
**Date:** 2026-09-02  
**Repository:** `MattiaRubino/dante`  
**Branch:** `feature/home-react`  
**Worktree:** `/home/mattia/projects/dante-frontend`

Read in order:

```text
1. world-focus-current-checkpoint.md
2. world-focus-substrate-closure-plan.md
3. world-focus-ws6-universal-work-primitives.md
4. world-focus-ws6-primitive-pressure-matrix.md
5. world-focus-substrate-final-convergence-proof.md
6. world-focus-substrate-combinatorial-evidence.md
7. world-focus-product-contract.md
8. world-focus-platform-contract.md
9. world-focus-frontend-roadmap.md
10. world-focus-evidence-index.md
```

Older `current next gate` wording is phase-time evidence where it conflicts with the checkpoint.

---

# 1. Current state

```text
WF0 route/shell                         FROZEN / USER AUTHORIZED
WF-G3 geometry                         LOCKED / USER AUTHORIZED
WF-V4 visual treatment                 CANDIDATE
B0 foundation                          ENGINEERING CLOSED
WR0–WR2                                CLOSED
B1 Orientation                         CLOSED FOR SEQUENCING
B2 Continuity / Resume                 IMPLEMENTED / AUTOMATED PASS
Workspace Platform                     ENGINEERING CLOSED
D0 DANTE spatial/presence              ACCEPTED
D1 quiet invoke/composer               CLOSED FOR SEQUENCING
WS0                                    CLOSED
WS1–WS5                                MAXIMAL PRACTICAL ANALYTICAL CLOSURE
WS6                                    CLOSED
WS7                                    NEXT ACTIVE GATE
WS8                                    NOT STARTED
M0–M7                                  BLOCKED UNTIL WS8
D2–D6                                  PRESERVED / MATERIALIZATION-DEFERRED
```

Workspace Platform:

```text
HEAD 6c441335a75bb913af8da1eda569d8094d38a539
CI 33549465793 PASS
500 composition scenarios
500 allocation/surface-stack scenarios
```

D1:

```text
HEAD f17291de32e6bdced20536807b32928ec1be6aea
CI 33552437179 PASS
```

Later analytical closure does not mean CG-32..CG-40 or final WP schemas are already implemented.

---

# 2. Product North Star

DANTE compass:

> **Understand life. Shape what comes next.**

World Focus compass:

> **Understand this part of my life and continue from here.**

World:

> **A user-recognizable continuity context for a significant part of reality.**

Core thesis:

> **A World is a shared coordinate system between the user and DANTE for one meaningful continuity context — not a shared source of truth.**

World is not a Domain owner, folder, taxonomy, dashboard ontology, DB partition, security boundary, AI memory bucket, chat room or mandatory Goal/time/KPI surface.

Worlds overlap; one canonical reality may appear through several bounded projections without duplication.

---

# 3. Output Grammar

```text
O1 Orientation
O2 Situation
O3 Continuity / Resume
O4 Attention / Resolution
O5 Next
O6 Change
O7 Trajectory / Comparison
O8 Evidence / History
O9 Explore
O10 Act / Decide
O11 Intelligence
```

A World need not answer all eleven.

```text
Output family != L1 primitive != module != renderer
```

Sparse/empty is valid. No fake KPI, urgency, AI prose or generic cards.

---

# 4. Higher-authority barriers

Preserve at minimum:

```text
Person != Account != Principal != Actor
Subject != Resource != native identity
Routine != Recurrence != Occurrence
Occurrence != Schedule != Session != Actual
Actual != Observation != Outcome
Evidence != Provenance
Confirmation != Acknowledgement
Authority != Visibility
Agreement != Consent
Responsibility != Participation
accepted Participation != Actual Participation
provider state != canonical state
derived projection != canonical truth
absence/unknown != false
idempotency != semantic identity
```

No universal Entity/Thing, Relationship/Edge, Fact/Version root or canonical EAV/property bag.

Protected-main Logical Model is CLOSED and retains `WL-H01..WL-H12`; World traceability is 12/12.

---

# 5. Frozen structure / existing platform

WF0:

```text
APP SHELL / GLOBAL TOPBAR
└ route outlet
   └ /worlds/:worldId
      └ WORLD FOCUS SHELL
         ├ visual frame
         ├ rectangular workspace
         └ shell controls
```

Global Topbar is outside World ownership.
WF-G3 is locked.

Reuse the existing Workspace Platform:

```text
stable/adaptive/ephemeral composition
finite module/surface registries
12-unit composition planner
workspace reducer
singular bounded selection/ref TODAY
interaction generation
surface open/replace/promote/close
blocking-tail barrier
expectedGeneration stale-intent guard
full/split allocation
overlay/focus + interactive/inert
sidecar/overlay/focus/dormant/external
ResizeObserver actual allocation
container-query ownership
local failure isolation
```

Do not create a second platform in WS7.

---

# 6. DANTE

D0 accepted:

> **AI availability is persistent; AI footprint is not.**

D1 global invoke remains:

```text
contextReference: null
```

No implicit selected-item leakage.

D2–D6 are later M4 work.

Future DANTE consumes bounded explicit refs, authorized minimized context, basis validity, revocation/redaction, identity resolution and operation revalidation. It does not own those semantics.

---

# 7. WS1–WS5 final proof

Authority:

`world-focus-substrate-final-convergence-proof.md`

Final chain:

```text
A domain/user-job           -> CG-32
B mechanism-first           -> CG-33..CG-36
C metamorphic               -> clean
D architecture mutants      -> 48/48 killed
E Logical traceability      -> 12/12 + CG-37..CG-40
F combinatorial             -> 4455/4455 + 2835/2835
G post-hardening            -> 48/48 + 18/18 killed
H future substitutions      -> 18/18, 0 new layers
```

Final concern ledger:

```text
CG-01..CG-40
```

Strict reopen only on a concrete final-proof counterexample. New provider/UI/World noun is not enough.

---

# 8. Layered substrate

```text
L0 Higher Authorities
L1 Work-Semantic Projections
L2 Evidence / Basis
L3 Coordination / Disclosure
L4 Interaction / Reference
L5 Composition Configuration
L6 Operation / Effect Presentation
L7 Renderer / Specialist Extension
L8 Platform / User Policies
```

DANTE consumes; DANTE does not own.

---

# 9. WS6 — CLOSED

Semantic authority:

`world-focus-ws6-universal-work-primitives.md`

Pressure matrix:

`world-focus-ws6-primitive-pressure-matrix.md`

Final L1:

```text
WP-01 CONTINUITY
WP-02 ATTENTION
WP-03 COMPARISON
WP-04 TRAJECTORY
```

## WP-01 Continuity

```text
recent != resumable
last viewed != meaningful checkpoint
open != in motion
unfinished != important
```

Meaning: truthful resumable thread + meaningful checkpoint + continuation seam when real.

## WP-02 Attention

```text
notification/read/dismiss/snooze/ack != semantic resolution
```

Meaning: materially unresolved matter + grounded reason/consequence + pending response/resolution.

## WP-03 Comparison

Meaning: 2+ explicit bounded alternatives/states/bases aligned across explicit dimensions.

```text
Change = from/to Comparison
Planned-vs-Actual = Comparison + Domain
Decision preparation = Comparison + Domain Decision/Proposal/Criterion + L2/L6
Comparison != winner/Decision/Proposal/source precedence
```

## WP-04 Trajectory

Meaning: ordered evolution of a bounded facet over explicit axis/window with truthful missingness/aggregation/basis.

```text
Trajectory != Register
missing != zero
trend != causality/prediction/progress automatically
```

---

# 10. Things deliberately NOT in L1

```text
Dependency / Requirement / Allocation / Session
Decision / Proposal / Request / Conditional Policy / Reconciliation
-> Domain

Evidence/provenance/basis/integrity/validity
-> L2

coordination/disclosure
-> L3

selection/ref resolution
-> L4

pin/saved view/config
-> L5

effect lifecycle
-> L6

collection/list/table/chart/timeline/pipeline/breakdown/map
-> L7 renderer/query/specialist

offline/sync/scale/a11y/responsive/performance
-> L8
```

Quick capture is interaction/application.
Review queue is a view over Attention.
Template is configuration.

A valid World may use **zero L1 primitives** if direct Domain/application projections are sufficient.

---

# 11. WS6 closure evidence

```text
44/44 primary scenarios mapped
40/40 Cycle-B mechanism scenarios classified
0 Cycle-C new L1 classes
40/40 CG ownership preserved
11/11 Output Grammar mapped
4/4 deletion mutants killed
6/6 pairwise primitive merges rejected
0 fifth primitive required
0 Domain duplicate accepted
0 generic root
0 DANTE-required primitive
0 page-per-World requirement
```

Unknown apiary-like World is expressible using WP-01..04 + direct Domain projections + optional L7 specialist.

---

# 12. WS7 — CURRENT NEXT GATE

> **Executable Non-Visual Harness**

Goal:

> Turn WP-01..04 + CG-01..40 into executable deterministic/property/state-machine/combinatorial proof while reusing the actual Workspace Platform.

Required inputs:

```text
WP-01..WP-04
CG-01..CG-40
WL-H01..WL-H12 carry-forward
67 fixed 3-way covering rows
157 fixed high-risk 4-way covering rows
existing 500+500 Workspace stress suites
```

Primitive tests:

```text
WP-01 checkpoint/resumability/no fake continue
WP-02 grounded unresolved matter + dismissal != resolution
WP-03 explicit refs/basis/incomparability/no automatic winner
WP-04 ordered axis/missingness/aggregation/no causal-progress inference
```

Cross-layer tests:

```text
freshness/coverage/integrity/validity/redaction
recipient/purpose non-interference
revocation + late result
primary/supporting refs + retirement ambiguity
config revision/concurrency/pinning
queued execution-time revalidation
partial/cancel/reverse/compensate
provider ACK ambiguity
cross-World reuse
recurrence/occurrence/DST
specialist fallback
large-data bounding
offline/replay
responsive/a11y semantic invariance
```

Harness requirements:

```text
seeded/reproducible
bounded CI runtime
minimal counterexamples where possible
named invariants
no arbitrary payload bag
no test-only semantic owner
no fake backend
no second reducer/planner
```

---

# 13. WS8 / materialization

WS8 runs final hostile executable falsification after WS7.

Only after WS8:

```text
M0 mapping/freeze
M1 core non-visual production materialization
M2 shared visual primitive layer
M3 adaptive World composition
M4 contextual DANTE — resume D2–D6
M5 contrasting complete Worlds
M6 integrated product/visual/a11y review
M7 pre-backend frontend freeze
```

M2 visual primitive names do not become L1 semantics.

---

# 14. Backend stop line

Before explicit later backend vertical:

```text
NO real World API for convenience
NO World persistence/Alembic
NO provider runtime
NO model routing/streaming
NO canonical chat persistence
NO durable Run backend
NO real tool/effect execution
NO fake success
```

---

# 15. Repository / continuation rule

Every branch write still requires exact branch/PRE-SCOPE/path gate and live HEAD check. No merge/rebase/force/main mutation without explicit authorization.

Immediate continuation:

> **WS7 only — build the executable oracle. Do not restart WS1–WS6 without their explicit counterexample triggers and do not materialize D2/UI yet.**
