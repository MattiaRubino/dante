# DANTE — World Focus Handoff

**Status:** CURRENT DURABLE HANDOFF — WS1–WS5 FINAL ANALYTICAL PROOF CLOSED / WS6 NEXT  
**Date:** 2026-09-02  
**Repository:** `MattiaRubino/dante`  
**Branch:** `feature/home-react`  
**Worktree:** `/home/mattia/projects/dante-frontend`

Read in order:

```text
1. world-focus-current-checkpoint.md
2. world-focus-substrate-closure-plan.md
3. world-focus-substrate-final-convergence-proof.md
4. world-focus-substrate-combinatorial-evidence.md
5. world-focus-ws0-substrate-inventory.md
6. world-focus-substrate-independent-confirmation.md
7. world-focus-substrate-convergence-review.md
8. world-focus-substrate-convergence-corpus.md
9. world-focus-frontend-roadmap.md
10. world-focus-evidence-index.md
```

Older `current next gate` prose is historical where it conflicts with the live checkpoint/program.

---

# 1. Current status

```text
WF0 route/shell                         FROZEN / USER AUTHORIZED
WF-G3 geometry                         LOCKED / USER AUTHORIZED
WF-V4 visual treatment                 CANDIDATE
B0 foundation                          ENGINEERING CLOSED
WR0–WR2                                CLOSED
B1 Orientation                         CLOSED FOR SEQUENCING
B2 Continuity / Resume                 IMPLEMENTED / AUTOMATED PASS
Workspace Platform                     ENGINEERING CLOSED
D0 DANTE spatial/presence              ACCEPTED
D1 quiet invoke/composer               CLOSED FOR SEQUENCING
WS0                                    CLOSED
WS1–WS5                                MAXIMAL PRACTICAL ANALYTICAL CLOSURE
WS6                                    NEXT ACTIVE GATE
WS7                                    NOT STARTED
WS8                                    NOT STARTED
M0–M7                                  BLOCKED UNTIL WS8
D2–D6                                  PRESERVED / MATERIALIZATION-DEFERRED
```

Workspace Platform evidence:

```text
HEAD 6c441335a75bb913af8da1eda569d8094d38a539
CI   33549465793 PASS
500 composition scenarios
500 allocation/surface-stack scenarios
```

D1 evidence:

```text
HEAD f17291de32e6bdced20536807b32928ec1be6aea
CI   33552437179 PASS
```

Do not turn analytical closure into a claim that later runtime hardenings are already implemented.

---

# 2. North Star / World product

DANTE compass:

> **Understand life. Shape what comes next.**

World Focus compass:

> **Understand this part of my life and continue from here.**

A World is:

> **A user-recognizable continuity context for a significant part of reality.**

Core thesis:

> **A World is a shared coordinate system between the user and DANTE for one meaningful continuity context — not a shared source of truth.**

World is not a Domain owner, folder, life taxonomy, dashboard ontology, universal container, DB partition, permission boundary, AI memory bucket, chat room or mandatory Goal/time/KPI surface.

Worlds may overlap. One canonical reality can appear through multiple bounded projections without duplication.

---

# 3. Output Grammar

```text
O1 Orientation
O2 Situation
O3 Continuity / Resume
O4 Attention / Resolution
O5 Next
O6 Change
O7 Trajectory / Comparison [optional]
O8 Evidence / History
O9 Explore
O10 Act / Decide
O11 Intelligence
```

A World need not answer all eleven. Sparse/empty is truthful; do not manufacture urgency, KPI or AI filler.

```text
Output family
!= automatically L1 primitive
!= automatically module
!= automatically renderer
```

---

# 4. Higher-authority barriers

Do not collapse:

```text
Person != Account != Principal != Actor
Subject != Resource != native identity
Routine != Recurrence != Occurrence
Occurrence != Schedule != Session != Actual
Actual != Observation != Outcome
Evidence != Provenance
Confirmation != Acknowledgement
Authority != Visibility
Agreement != Consent
Responsibility != Participation
accepted Participation != Actual Participation
provider state != canonical state
derived projection != canonical truth
absence/unknown != false
idempotency != semantic identity
```

No universal Entity/Thing, Relationship/Edge, Fact/Version root or canonical property bag.

Protected-main Logical Model is CLOSED and carries WL-H01..WL-H12. The final World proof maps 12/12 of them without creating a new Domain/Logical owner.

Physical PostgreSQL remains canonical persistence/material-history authority. World never owns persistence or access semantics merely because it is a route.

---

# 5. Frozen structure / existing platform

WF0 remains:

```text
APP SHELL / GLOBAL TOPBAR
└ route outlet
   └ /worlds/:worldId
      └ WORLD FOCUS SHELL
         ├ visual frame
         ├ rectangular workspace
         └ shell controls
```

Global Topbar is outside World ownership.

WF-G3 remains locked. Visual skin fits geometry; geometry does not chase visual skin.

Existing Workspace Platform must be reused:

```text
stable/adaptive/ephemeral composition
finite module/surface registries
12-unit planner
workspace reducer
singular bounded selection/ref TODAY
interaction generation
surface open/replace/promote/close
blocking-tail barrier
expectedGeneration stale-intent guard
full/split allocation
overlay/focus + interactive/inert
sidecar/overlay/focus/dormant/external
ResizeObserver real allocation
container-query ownership
local failure isolation
```

Future multi-reference and CG-32..CG-40 runtime behavior are not implemented yet.

---

# 6. DANTE disposition

D0 accepted:

> **AI availability is persistent; AI footprint is not.**

D1 closed for sequencing. Global invoke remains:

```text
contextReference: null
```

No implicit selected-item leakage.

D2–D6 remain Materialization M4 work.

Future contextual DANTE must consume bounded explicit refs and authoritative minimized context; it cannot create truth, permission or effect completion.

---

# 7. Final WS1–WS5 proof

Final authority:

`world-focus-substrate-final-convergence-proof.md`

Combinatorial evidence:

`world-focus-substrate-combinatorial-evidence.md`

Supporting A/B/C evidence remains in the earlier convergence documents.

Final proof chain:

```text
A domain/user-job                    -> CG-32 found
A regression                        -> regression only
B mechanism-first                   -> CG-33..CG-36 found
C metamorphic                       -> clean
D architecture mutants              -> 48/48 killed
E Logical traceability              -> 12/12 mapped, CG-37..CG-40 found
F N-way combinations                -> 4455/4455 + 2835/2835 covered
G post-last-hardening proof         -> 48/48 + 18/18 killed, 0 new classes
H future substitution proof         -> 18 substitutions, 0 new layers
```

Critical closure fact:

> **After the last material hardening (CG-40), two independent methods remained clean.**

Final concern ledger:

```text
CG-01..CG-40
```

---

# 8. Hardened ownership model

```text
L0 Higher Authorities
   Domain / Logical / Physical / authoritative application truth

L1 Work-Semantic Projections
   final finite vocabulary = WS6

L2 Evidence / Basis
   source/provenance/material basis
   freshness/coverage/conflict
   optional integrity/attestation
   validity/supersession/correction/retraction
   authorized tombstone/redaction continuity

L3 Coordination / Disclosure
   participant-facing state
   responsibility/acknowledgement
   selective disclosure/private overlay
   revision/scope-bound consent/binding applicability
   recipient/purpose disclosure basis
   derived-output non-interference

L4 Interaction / Reference
   active World/generation
   primary ref + bounded ordered supporting refs
   surface refs
   authoritative reference-resolution result
   retired/ambiguous safe degradation

L5 Composition Configuration
   stable/adaptive/ephemeral
   favorite/shortcut/saved query/view
   personal/shared config
   ownership/audience + revision/version/migration/concurrency

L6 Operation / Effect Presentation
   intent/Proposal/confirmation
   dispatch/queued/pending/ambiguous
   execution-time revalidation seam
   partial real effect
   cancel-before-effect
   reversal/refund/release
   compensation/repair
   reconciliation/receipt

L7 Renderer / Specialist Extension
   finite trusted registry + truthful fallback/local failure

L8 Platform / User Policies
   scale/offline/sync/replay
   accessibility/reduced motion
   notifications/performance/responsive/failure isolation
```

DANTE consumes these layers; it does not own them.

---

# 9. Latest carry-forward hardenings

```text
CG-37 queued/deferred consequential execution revalidation
CG-38 reference resolution under retirement/merge/split ambiguity
CG-39 redaction/tombstone/historical reference continuity
CG-40 recipient/purpose-bound derived-output non-interference
```

Key rules:

```text
queued earlier != executable later automatically
old ref != permission to choose successor
redacted != never existed
historical existence != old payload remains visible
recipient/purpose-A derivation != automatically reusable for B
aggregate/count/ranking/error/explanation can disclose hidden information
```

The authoritative upstream layer resolves identity, governance and safe projections; World presents/coordinates the resulting bounded states.

---

# 10. Strict reopen policy

WS1–WS5 reopens only for concrete evidence that:

```text
R1 real user job cannot fit L0–L8 without generic escape hatch;
R2 WS6/WS7/WS8 discovers a new semantic/ownership/state family;
R3 executable privacy/race/invariant failure cannot be repaired in an existing owner;
R4 accepted upstream Domain/Logical change contradicts a World assumption;
R5 specialist capability requires core ownership change rather than extension;
R6 required disclosure/non-interference cannot be preserved;
R7 consequential operation cannot fit effect/revalidation/reconciliation boundaries truthfully.
```

Not reopen evidence:

```text
new World/domain noun
new provider/API
backend storage change
new DANTE model/provider
new config/sync engine
new renderer or viewport
more data
another competitor pattern
```

Reopen requires a counterexample, not curiosity.

---

# 11. WS6 — CURRENT NEXT GATE

Question:

> **What is the smallest finite L1 work-semantic vocabulary that expresses the final proof without duplicating Domain semantics or creating a generic escape hatch?**

Candidate pressure only:

```text
attention / unresolved work
choice / decision preparation
measurement / series
movement / balance / reconciliation
dependency / requirement
execution/session projection
pipeline/stage
capture/review
```

Each candidate must become exactly one:

```text
UNIVERSAL WORK PRIMITIVE
COMPOSITION OF SMALLER PRIMITIVES
EXISTING DOMAIN PROJECTION PATTERN
INTERACTION / CONFIGURATION PATTERN
SPECIALIST EXTENSION
REJECTED UNIVERSALIZATION
```

Aggressive deletion/merge pressure is mandatory. L2–L8 do not become L1 merely to simplify naming.

WS6 is non-visual. Do not start D2 or final UI.

---

# 12. WS7 / WS8 / materialization

WS7 turns WS6 + final convergence proof into deterministic executable assertions and must consume or reproduce the fixed covering vectors.

WS8 performs final hostile executable falsification. A material failure returns only to the earliest necessary phase.

After WS8:

```text
M0 Materialization Mapping / Scope Freeze
M1 Core Non-Visual Production Materialization
M2 Shared Visual Primitive Layer
M3 Adaptive World Composition
M4 Contextual DANTE — preserve D0/D1, resume D2–D6
M5 Contrasting Complete Worlds
M6 Integrated Product / Visual / Accessibility Review
M7 Pre-Backend Frontend Freeze
```

---

# 13. Backend stop line

Before explicit later backend work:

```text
NO real World API merely for frontend completeness
NO World DB/Alembic persistence
NO provider runtime
NO real model routing/streaming
NO canonical chat persistence
NO durable Run backend
NO real tool/effect execution
NO fake success
```

---

# 14. Repository discipline / continuation

Authority order:

```text
protected-main accepted executable/docs
current main docs
active bounded branch record for its own scope
other branch evidence
Git history
conversation
```

Every remote write requires exact branch/PRE-SCOPE/path gate and live HEAD check. No merge/rebase/force/main mutation without explicit authorization.

Immediate next instruction:

> **WS6 only — close L1 from the final A→H proof and CG-01..CG-40. Do not restart WS1–WS5 unless a strict reopen trigger is proven.**
