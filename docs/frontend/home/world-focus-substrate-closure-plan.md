# DANTE — World Focus Substrate Closure Program

**Status:** CURRENT PROGRAM — WS0 CLOSED / WS1–WS5 INDEPENDENT CONVERGENCE CLOSED / WS6 NEXT  
**Date:** 2026-09-02  
**Branch:** `feature/home-react`  
**Scope:** pre-materialization closure of the reusable non-visual World substrate before further World/DANTE surface materialization.

This document is the sequencing authority for the **World Substrate Closure Program**.

It does not reopen the accepted World definition, WF0 frozen shell, WF-G3 locked geometry, D0 DANTE spatial direction, D1 entry behavior, B1 Orientation or B2 Continuity semantics.

The previously planned D2–D6 work remains preserved for later Materialization M4. It is not the current gate.

---

## 1. Program thesis

```text
Workspace Platform engineering closed
!=
World substrate universally falsified and closed
```

The closure program separates:

```text
WS0–WS8   substrate discovery / falsification
M0–M7     frontend materialization
LATER     real backend / provider / LLM / governed effects
```

The goal is to close the smallest reusable World substrate before visual implementation makes weak abstractions expensive to remove.

---

## 2. Permanent authority boundaries

All WS phases preserve:

```text
World != canonical Domain owner
WorldProjection != canonical truth
World relevance != authorization
surface visibility != disclosure permission
AI output != accepted fact
Proposal != Decision != effect
provider acknowledgement != canonical completion
planned/intended != Actual
absence/unknown != false
Evidence != Provenance
Authority != Visibility
Ownership != Possession
Routine != Recurrence != Occurrence
Occurrence != Schedule != Session != Actual
idempotency != semantic identity
```

Higher authority remains with Domain / Logical / Physical / AuthZ / governed application/runtime layers.

Never create for frontend convenience:

```text
universal Entity / Thing
universal Relationship / Edge
universal Fact / Version root
canonical EAV/property bag
World DB/ACL ownership
arbitrary AI executable UI
```

---

## 3. Current sequence

```text
WS0 — Substrate Inventory
      CLOSED

WS1–WS5 — WORLD SUBSTRATE CONVERGENCE LOOP
      ANALYTICALLY CLOSED
      after independent Cycle B + metamorphic Cycle C confirmation

WS6 — Universal Work Primitive Closure
      NEXT ACTIVE GATE

WS7 — Executable Non-Visual Harness
      NOT STARTED

WS8 — Final Falsification
      NOT STARTED

ONLY AFTER WS8:
M0–M7 Materialization
```

WS1–WS5 remain one indivisible macro-block; their numbers label activities, not five separate delivery gates.

---

## 4. WS0 — Substrate Inventory

**CLOSED BASELINE**

Authority:

`world-focus-ws0-substrate-inventory.md`

Main result:

```text
workspace/composition/surface mechanics = substantially executable
real reusable work semantics            = partially materialized
broad work-pattern knowledge            = research-evidenced
universal work primitive vocabulary     = open
whole-corpus executable oracle          = open
```

Existing Workspace Platform closure remains valid:

```text
HEAD 6c441335a75bb913af8da1eda569d8094d38a539
CI   33549465793 PASS
```

---

## 5. WS1–WS5 — corrected convergence methodology

### 5.1 Original Cycle A

Durable evidence:

```text
world-focus-substrate-convergence-corpus.md
world-focus-substrate-convergence-review.md
```

Cycle A was user/domain driven:

```text
44 primary scenarios
+ WITHOUT/WITH DANTE
+ 16 adversarial cross-products
-> discovers CG-32 revocation/disclosure invalidation
-> harden
```

The later 44-scenario rerun + 20 extra cases remains useful **regression evidence**.

It is no longer treated by itself as the independent confirmation required for final convergence closure.

### 5.2 Why the methodology was corrected

The user correctly identified:

> Replaying mostly the same scenario space after hardening proves regression resistance, not strong independent falsification.

Therefore a valid convergence closure now requires method diversity:

```text
scenario/domain diversity
+
mechanism-first diversity
+
metamorphic/invariant diversity
```

### 5.3 Cycle B — independent mechanism-first attack

Authority:

`world-focus-substrate-independent-confirmation.md`

Cycle B deliberately starts from difficult mechanics and orthogonal domains rather than from Cycle-A World families.

Evidence:

```text
40 mechanism-first scenarios
40 WITHOUT/WITH DANTE evaluations
16 independent mechanism cross-products
```

Cycle B exposed four additional material classes:

```text
CG-33 scope/revision-bound approval/consent/binding validity
CG-34 evidence integrity/attestation != provenance/freshness
CG-35 cancel != reverse/refund/compensate after real/partial effect
CG-36 basis validity/supersession/retraction != freshness
```

The architecture was hardened before confirmation continued.

### 5.4 Cycle C — metamorphic confirmation

Cycle C removes domain storytelling as the main variable and mutates the model itself.

Evidence:

```text
40 metamorphic/invariant transformations
```

Pressure includes:

```text
subject revision after approval
eligible actor/quorum changes
resource scope changes
revocation during mounted/in-flight state
freshness change vs value change
correction/retraction
integrity unknown/failure
partial effect
cancel-before vs cancel-after
compensation failure
idempotent retry
World switch / late result
primary/supporting ref mutations
config concurrency
pinned derived result invalidation
recurrence rule / occurrence exception / DST
large-history scaling
specialist loss
a11y/responsive transformation
```

Cycle C result:

```text
new material work-primitive class                 0
new substrate ownership layer                    0
new interaction-state class                      0
new unexpressible presentation/surface class      0
new Domain/Logical/Physical contradiction         0
new DANTE/non-DANTE substrate class               0
new multi-actor/privacy/disclosure class          0
new effect/governance presentation class          0
new evidence/basis class                          0
new failure/race/responsive/a11y substrate class  0
```

Therefore:

> **WS1–WS5 WORLD SUBSTRATE CONVERGENCE LOOP — ANALYTICALLY CLOSED AFTER INDEPENDENT CONFIRMATION.**

---

## 6. Hardened layered substrate entering WS6

```text
L0 Higher authorities
   Domain / Logical / Physical / AuthZ / governed application truth

L1 Work-semantic projections
   finite reusable work meanings — WS6 closes these

L2 Evidence / basis envelope
   source / provenance / material basis
   as-of / freshness
   coverage / missingness
   conflict / unresolved
   optional integrity / attestation
   validity / supersession / correction / retraction

L3 Coordination / disclosure envelope
   participant state
   responsibility / acknowledgement
   selective disclosure
   shared fact + private overlay
   revision/basis-scoped approval/consent/binding applicability where required

L4 Interaction / cursor state
   World / generation / primary ref / bounded supporting refs / active surface

L5 Composition configuration
   stable/adaptive/ephemeral
   personal favorite/shortcut
   saved query/view
   personal/shared config
   revision/version/migration/concurrency

L6 Operation / effect presentation
   intent / Proposal / confirmation
   dispatch / pending / ambiguity
   cancel-before-effect where meaningful
   partial effect
   reversal/refund/release
   compensation/repair
   reconciliation / receipt

L7 Renderer / specialist extension
   finite code-shipped catalog + truthful fallback

L8 Platform / user policies
   scale / offline / replay / a11y / notifications / performance / responsive
```

DANTE consumes this structure. It does not own or authorize it.

---

## 7. High-value convergence decisions

The full `CG-01..CG-36` record spans the Cycle-A review plus independent confirmation.

Important hardenings include:

```text
meaningful Change needs an explicit reference basis
evidence lifecycle status != full evidence basis
attention read/dismiss != semantic resolution
comparison/recommendation != Proposal automatically
Proposal != Decision
resource requirement != candidate != binding != Actual use
participant acceptance != authority != Actual participation
personal favorite != pinned config != saved view != source
provider ACK != canonical completion
conflicting evidence may remain unresolved
primary deictic ref + bounded supporting refs
revoked disclosure invalidates mounted/in-flight eligibility
approval/consent/binding may need exact revision/basis applicability
provenance != evidence integrity/attestation
cancel != reversal/refund/compensation
freshness != basis validity/supersession/retraction
```

Explicit non-universalizations remain:

```text
TemporaryMode
universal World Lens
offline/sync as work primitive
large-data as work primitive
a11y/low-attention as work ontology
WorldItem / Thing / Widget / Fact / generic Relationship
```

---

## 8. WS6 — Universal Work Primitive Closure

**NEXT ACTIVE GATE**

Question:

> What is the smallest finite **L1 work-semantic vocabulary** that expresses the independently converged corpus without absorbing L2/L3/L4/L5/L6/L8 into a giant generic frontend object system?

Candidate pressure clusters — not pre-approved primitives:

```text
attention / unresolved work
choice / decision preparation
measurement / series
movement / balance / reconciliation
dependency / requirement
execution/session projection
pipeline/stage
capture/review
```

Every candidate must be classified as one of:

```text
UNIVERSAL WORK PRIMITIVE
COMPOSITION OF SMALLER PRIMITIVES
EXISTING DOMAIN PROJECTION PATTERN
EVIDENCE / COORDINATION ENVELOPE
INTERACTION / CONFIGURATION PATTERN
EFFECT PRESENTATION PATTERN
SPECIALIST EXTENSION
PLATFORM POLICY
REJECTED UNIVERSALIZATION
```

### WS6 deletion pressure

For every candidate:

```text
Can it be removed?
Can it be composed from smaller meanings?
Is it actually Domain semantics?
Is it only an evidence/basis concern?
Is it coordination/config/effect state instead of work semantics?
Does it require DANTE to exist?
Does it solve multiple materially different scenarios?
Would keeping it create a generic escape hatch?
```

Keep only what survives.

### WS6 exit

```text
finite L1 primitive catalog
explicit semantics + non-semantics
full mapping to Cycle A/B/C evidence
no unexplained overlap
specialist boundary explicit
no generic escape hatch
DANTE optionality intact
Domain/Logical/Physical boundaries intact
```

No final UI is required.

---

## 9. WS7 — Executable Non-Visual Harness

WS7 turns analytical invariants into deterministic executable proof.

Required original obligations remain plus new Cycle-B/C cases:

```text
primitive applicability
absence/unresolved outputs
stable/adaptive/ephemeral composition
basis/freshness propagation
primary/supporting refs
revocation invalidation
late-result rejection
cross-World reuse
specialist fallback
approval bound to exact subject revision
resource binding invalidated after scope mutation
integrity unknown vs integrity failed
superseded/retracted basis invalidating current dependent output
cancel before effect
cancel after partial effect
compensation as separate governed operation
idempotent retry != compensation
recurrence series + exception + DST
pinned derived result whose basis becomes invalid
```

Analytical convergence is not executable proof.

---

## 10. WS8 — Final Falsification

Run the complete corpus and generated high-risk combinations against the executable substrate.

Closure requires zero unresolved material gap, no generic escape hatch, no page-per-World requirement, no AI-only basic path and no unresolved Domain/privacy/state contradiction.

A real failure returns to the earliest necessary WS phase.

---

# 11. Materialization M0–M7 — only after WS8

```text
M0 Materialization Mapping / Scope Freeze
M1 Core Non-Visual Production Materialization
M2 Shared Visual Primitive Layer
M3 Adaptive World Composition Materialization
M4 Contextual DANTE Materialization
M5 Contrasting Complete Worlds
M6 Integrated Product / Visual / Accessibility Review
M7 Pre-Backend Frontend Freeze / Handoff
```

M4 preserves D0/D1 and resumes D2–D6.

M6 subsumes the former D7 integrated review.

---

## 12. Backend stop line

Until explicit backend vertical:

```text
NO real World business API
NO World persistence/Alembic
NO provider runtime
NO real model routing/streaming
NO canonical chat persistence
NO durable DANTE Run backend
NO real tool/effect execution
NO fake successful effects
```

---

## 13. Documentation discipline

Never collapse:

```text
regression rerun
!= independent convergence confirmation
!= primitive closure
!= executable proof
!= frontend materialization
!= human visual acceptance
!= backend/runtime proof
```

Current read order:

```text
1. world-focus-current-checkpoint.md
2. world-focus-substrate-closure-plan.md
3. world-focus-ws0-substrate-inventory.md
4. world-focus-substrate-independent-confirmation.md
5. world-focus-substrate-convergence-review.md
6. world-focus-substrate-convergence-corpus.md
7. world-focus-handoff.md
8. world-focus-frontend-roadmap.md
9. product/platform/structure/geometry contracts
10. evidence index
```

---

## 14. Current disposition

```text
WS0 Substrate Inventory                    CLOSED
WS1–WS5 Substrate Convergence Loop         ANALYTICALLY CLOSED — independently confirmed
WS6 Universal Work Primitive Closure       NEXT
WS7 Executable Non-Visual Harness          NOT STARTED
WS8 Final Falsification                    NOT STARTED
M0–M7 Materialization                      BLOCKED UNTIL WS8
D2–D6                                      PRESERVED / MATERIALIZATION-DEFERRED
```

Immediate next action:

> **WS6 only — close the finite L1 universal work-semantic vocabulary from the complete Cycle A + independent Cycle B + metamorphic Cycle C evidence.**
