<!-- LIFEOS-CANONICAL-CONTINUATION document="domain-model.md" follows="domain-model-part-18.md" -->
> **Canonical continuation of the single logical Domain Model workstream document.** Earlier closure/readiness milestones remain preserved; this continuation records the later second-V3 safety rerun and targeted Living Referent repair.

# 2026-08-16 — Targeted semantic reopen after North Star safety rerun

Current working baseline before this propagation scope:

```text
feature/domain-model
a262fd5d38baed6be136696385e04c62c0609a53
```

The requested second V3 safety pass re-ran the semantic kernel against:

- the accepted North Star;
- full inverse reconstruction / necessity testing;
- product and adversarial simulations;
- missing-concept discovery;
- adjacent/external product pressure under the existing evidence-only rule.

One bounded required gap was demonstrated:

```text
Living Referent
persistent native identity for individually tracked
non-human living organisms/specimens
```

This is a true semantic reopen, not merely an implementation/readiness hardening.

Accepted semantic result:

```text
LIVING REFERENT v0
PASS WITH HARDENING

NEW NATIVE REFERENT
YES
```

The repair preserves all existing boundaries:

```text
Living Referent != Person
Living Referent != Asset
Living Referent != Subject
Living Referent != Resource

Pet / Animal / Plant
NOT separate universal kernel roots
```

Current workstream state:

```text
Living Referent semantic review       COMPLETE
propagation                           IN PROGRESS
remote propagation QA                PENDING
Living Referent closure               CONDITIONAL ON QA
fresh Whole-Domain WD-01..10 rerun    REQUIRED AFTER CLOSURE
Logical Model                         HOLD
SQL/API/backend implementation        NOT AUTHORIZED
```

No unrelated semantic area is reopened by convenience. The only allowed next semantic work after this repair is the already requested product hardening and full final V3/WD safety rerun before logical-model authorization.

Normative references:

- `../domain/concepts/living-referent.md`;
- `../domain/checkpoints/living-referent-v0-validation.md`;
- `../domain/checkpoints/whole-domain-audit-v0-part-6.md`.
